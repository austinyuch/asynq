vendor
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Ignore examples for now
/examples

# Ignore tool binaries
/tools/asynq/asynq
/tools/metrics_exporter/metrics_exporter

# Ignore asynq config file
.asynq.*

# Ignore editor config files
.vscode
.idea

# Local tooling artifacts (not for version control)
.code-review/
temp/
go.work
go.work.sum

# Local security CI evidence (SBOM / CVE / KEV snapshots, reports, fix plan).
# Regenerate with `make security`; see docs/SECURITY_LOCAL_CI.md.
.security/




# cross-agents symlink bridge: managed begin
.claude/skills
.kiro/skills
.codex/skills
# cross-agents symlink bridge: managed end


# cross-agents symlink bridge: claude-md begin
CLAUDE.md
internal/CLAUDE.md
internal/rdb/CLAUDE.md
tools/asynq/CLAUDE.md
tools/asynq/cmd/CLAUDE.md
tools/CLAUDE.md
x/CLAUDE.md
# cross-agents symlink bridge: claude-md end
